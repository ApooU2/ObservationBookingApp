{
  "name": "observatory-booking-frontend-deploy",
  "version": "1.0.0",
  "description": "Frontend deployment configuration for Vercel",
  "private": true,
  "scripts": {
    "build": "cd apps/frontend && npm install && npm run build",
    "start": "cd apps/frontend && npm start",
    "install": "cd apps/frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
